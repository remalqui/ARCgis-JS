import {
  A,
  s2 as s
} from "./chunk-I4U7MQNO.js";

// node_modules/@arcgis/core/geometry/support/Indices.js
function t(t2) {
  return s(t2) ? t2.length < A ? t2 : e(t2) : t2.length < A ? Array.from(t2) : t2.BYTES_PER_ELEMENT === Uint16Array.BYTES_PER_ELEMENT ? t2 : e(t2);
}
function e(n) {
  let t2 = true;
  for (const e2 of n) {
    if (e2 >= 65536)
      return s(n) ? new Uint32Array(n) : n;
    e2 >= 256 && (t2 = false);
  }
  return t2 ? new Uint8Array(n) : new Uint16Array(n);
}
function i(r) {
  return r <= A ? new Array(r) : r <= 65536 ? new Uint16Array(r) : new Uint32Array(r);
}
var a = (() => {
  const r = new Uint32Array(131072);
  for (let n = 0; n < r.length; ++n)
    r[n] = n;
  return r;
})();
var u = [0];
var o = (() => {
  const r = new Uint16Array(65536);
  for (let n = 0; n < r.length; ++n)
    r[n] = n;
  return r;
})();
function y(r) {
  if (1 === r)
    return u;
  if (r < A)
    return Array.from(new Uint16Array(o.buffer, 0, r));
  if (r < o.length)
    return new Uint16Array(o.buffer, 0, r);
  if (r > a.length) {
    const n = Math.max(2 * a.length, r);
    a = new Uint32Array(n);
    for (let r2 = 0; r2 < a.length; r2++)
      a[r2] = r2;
  }
  return new Uint32Array(a.buffer, 0, r);
}
var A2 = new Uint8Array(65536);

export {
  t,
  i,
  y
};
//# sourceMappingURL=chunk-L7P4URYM.js.map
